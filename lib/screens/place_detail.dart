import 'package:flutter/material.dart';

class Place_Detail extends StatelessWidget {
  const Place_Detail({Key key}) : super(key: key);
  static const routename = "./place_detial";
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
